import styled from '@emotion/styled

export const Header = styled.div`
	position: relative;
`

export const StickyHeader = styled.div`
	position: sticky;
	top: 0;
	z-index: 15;
	nav {
		height: 4rem;
	};
`;
